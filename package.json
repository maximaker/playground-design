{
  "name": "playground-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev --workspace=@playground/server",
    "dev:web": "npm run dev --workspace=@playground/web",
    "build": "npm run build:web",
    "build:web": "npm run build --workspace=@playground/web",
    "start": "npm run start --workspace=@playground/server",
    "typecheck": "tsc -p packages/shared/tsconfig.json --noEmit && tsc -p packages/server/tsconfig.json --noEmit && tsc -p packages/web/tsconfig.json --noEmit",
    "test": "npm run test --workspace=@playground/shared && npm run test --workspace=@playground/server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22"
  }
}