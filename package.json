{
  "name": "canvas-monorepo",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev --workspace=@canvas/server",
    "dev:web": "npm run dev --workspace=@canvas/web",
    "build": "npm run build:web",
    "build:web": "npm run build --workspace=@canvas/web",
    "start": "npm run start --workspace=@canvas/server",
    "typecheck": "tsc -p packages/shared/tsconfig.json --noEmit && tsc -p packages/server/tsconfig.json --noEmit && tsc -p packages/web/tsconfig.json --noEmit",
    "test": "npm run test --workspace=@canvas/shared && npm run test --workspace=@canvas/server"
  },
  "devDependencies": { "npm-run-all": "^4.1.5", "typescript": "^5.7.2" }
}
